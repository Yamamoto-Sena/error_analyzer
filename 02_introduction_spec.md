# 起動画面・UI/UX仕様書

**対象ファイル**: `src/App.tsx`（ヘッダー部分）, `src-tauri/tauri.conf.json`
**目的**: 初めてDebug Buddyを起動する新人エンジニアでも安心でき、開発のモチベーションが高まる「かっこよくて親しみやすい」画面を定義する。

> 本書は旧版でCLI（ターミナルASCIIバナー）を前提に記述されていましたが、実装は Tauri + React によるGUIデスクトップアプリとして進行しているため、実際の画面構成に合わせて全面的に書き改めています。

---

## 1. アプリ起動時の画面イメージ

ウィンドウサイズは 1120×780（最小 900×650）。タイトルバーには「Debug Buddy - AI-Powered Debugging Assistant」と表示される（`tauri.conf.json`）。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 🖥  DEBUG BUDDY                                [Gemini: gemini-3.5-flash-lite ▾] │
│    AI-Powered Debugging & Error Log Analysis Assistant 🚀                     │
│                  [🔑 Gemini AI: APIキー設定] [🖳 ターミナル監視] [📋 クリップボード監視] │
│                       [📁 プロジェクトフォルダ未選択] [🕘 解析履歴] [🌙]        │
├───────────────────────────────┬────────────────────────────────────────────┤
│  左ペイン: 入力                │  右ペイン: 解析結果                          │
│  ・エラーログ/スタックトレース  │  タブ: [根本原因&要約] [修正案(Diff)] [学習メモ]│
│  ・症状の説明（任意）           │  結果がない間はプレースホルダー＋サンプル       │
│  ・スクリーンショット添付       │  ボタン（TypeError等5種類）を表示             │
│  ・[エラーを解析する ▶]        │                                              │
└───────────────────────────────┴────────────────────────────────────────────┘
```

> 旧版ではヘッダーに「Desktop v0.1.0」バージョンバッジ・常設の「モデル診断」ボタンがあり、
> ヘッダー下には「合言葉」を掲げたウェルカムバナー（サンプルボタン群を含む）を配置していたが、
> 常設の場所を取るだけで使用頻度が低い要素だったため撤去・移設した。モデル診断はAPIキー設定
> モーダルの中（キー設定済みのときだけ表示）へ、サンプルボタンは右ペインの「まだ解析結果が
> 無い」プレースホルダー内（結果が出ると自動的に隠れる）へ、それぞれ移設している。

---

## 2. デザイン・演出要素の仕様

### 2.1 ロゴ・ヘッダー
- **ロゴ**: シアン→ティール→インディゴのグラデーション角丸アイコン内に`Terminal`アイコン（lucide-react）。
- **アプリ名**: 「DEBUG BUDDY」をシアン→ティールのグラデーションテキストで表示。
- **サブタイトル**: 「AI-Powered Debugging & Error Log Analysis Assistant 🚀」。
- **ヘッダーは画面上部に固定（sticky）**し、半透明の背景＋ぼかし（backdrop-blur）でスクロール時も視認性を確保。

### 2.2 ヘッダー右側のコントロール群
左から順に以下を配置する。使用頻度の低い要素（モデル診断・サンプルボタン等）は常設ボタンにせず、関連する操作の文脈（APIキー設定モーダル・結果が無い状態のプレースホルダー）に移設し、ヘッダーは日常的に使う要素だけに絞っている。
1. **Geminiモデル選択ドロップダウン**: CPUアイコン付き。APIキー未設定時は既定のモデル一覧、設定時はAPIから動的取得した一覧を表示。
2. **Gemini AI: APIキー設定ボタン**: 未設定時はグレー、設定済み（Active）時はエメラルドグリーンでハイライト。設定済みの場合、モーダル内に「登録済みモデルを診断する」ボタンが表示される（→2.2.1）。
3. **本日のトークン消費量表示**: Gemini APIを1回でも呼び出した場合のみ表示。Google側の日次クォータ(RPD)がリセットされる太平洋時間(PT)の深夜0時を集計境界とし、localStorageで永続化する（アプリを再起動しても本日分は保持される）。本日中にいずれかのモデルで実際に日次上限超過(429)を検知していればアンバー表示＋警告アイコンになる。ツールチップにモデル別の内訳を表示。
4. **ターミナル監視ボタン**: デスクトップアプリ版でのみ活性化。Web版ではグレーアウトし、ツールチップで理由を明示。
5. **クリップボード監視ボタン**: デスクトップアプリ版でのみ活性化。実行中は緑色＋点滅ドット＋「監視中」表示に変化し、モーダルを閉じていても一目で状態が分かる。Web版ではグレーアウトし、ツールチップで理由を明示。
6. **プロジェクトフォルダ選択ボタン**: 未選択時は「プロジェクトフォルダ未選択」、選択済みはフォルダ名を表示（エメラルド強調）。選択済みの場合のみ、隣にバックアップ一括削除ボタンと選択解除ボタンが並ぶ。
7. **Git未コミット変更バッジ**: 未コミットの変更がある場合のみ、アンバー色で件数を表示。
8. **解析履歴ボタン**: インディゴで強調表示し、履歴件数をバッジ表示。最も見つけやすい位置・配色にする。
9. **ライト/ダークモード切替ボタン**: 太陽/月アイコンをトグル。

#### 2.2.1 モデル診断（APIキー設定モーダル内）
「一覧には出ているのに実際には呼べないモデルがある」場合の切り分け用機能。APIキー設定モーダルを開いたとき、キー設定済みであれば入力欄の下に「登録済みモデルを診断する」ボタン（点線枠）を表示する。押すとAPIキー設定モーダルを閉じてモデル診断モーダルを開き、「診断を開始」でドロップダウン上の各モデルへ実際に軽量なリクエストを1件ずつ送り、利用可否を一覧表示する。

### 2.3 サンプルボタン（右ペインのプレースホルダー内）
まだ解析結果が無い状態の右ペインに、5種類（TypeError／SyntaxError／ReferenceError／Port競合(1420)／AttributeError(Python)）をエラー種別ごとに色分けして配置し、クリックで対応するサンプルログを左ペインへ即挿入する。解析結果が表示されるとこのプレースホルダーごと隠れるため、サンプルボタンも常設ではなく「必要な場面だけ出る」形になる。

### 2.4 システム状態の可視化（初心者が最も躓きやすいポイントへの配慮）
CLIツールで初心者が最も躓きやすいのが「APIキーの設定忘れ」です。GUI版では常時ヘッダーのボタン状態で明示する。

| 項目 | 正常時 | 未設定/異常時 | 案内 |
| :--- | :--- | :--- | :--- |
| **Gemini API** | ボタンがエメラルド色＋「Active」表示 | グレー表示＋「APIキー設定」 | クリックでモーダルを開き、Google AI Studioの無料キー取得リンクを提示 |
| **プロジェクトフォルダ**（実ファイル適用に必要） | エメラルド色＋フォルダ名表示 | グレー表示＋「プロジェクトフォルダ未選択」 | ツールチップで「選択すると実ファイルへの安全な自動適用が使えます」と案内 |
| **Git作業ツリー** | バッジ非表示 | アンバー色バッジで変更件数を表示 | ツールチップで「コミットまたは退避を推奨」と案内（処理はブロックしない） |

### 2.5 トースト通知
画面右下にアニメーション付きで表示し、4秒後に自動的に消える。
- **success**（緑）: 解析成功、適用成功、ロールバック成功など。
- **info**（青系）: マスキング件数の通知、フォールバック発生の案内など。
- **warning**（アンバー/赤）: 通信エラーによるローカル解析へのフォールバック、同一エラーの再発検知、コメントのみ貼り付け時の警告など。

### 2.6 右クリックメニュー
Tauriアプリの実体はWebView（内蔵ブラウザ）であるため、何も対処しないとブラウザ由来の「戻る/進む/再読み込み/検証」等のメニューがそのまま表示されてしまう。ローカルの1アプリとしての操作感に合わせるため、既定のメニューは常に表示せず、代わりに以下のみを自作メニューとして表示する。
- 入力欄（テキストエリア）でテキストを選択中: 「切り取り」「コピー」「貼り付け」
- 入力欄でカーソルのみ（未選択）: 「コピー」（無効表示）「貼り付け」
- 入力欄以外でテキストを選択中（解析結果の文章など）: 「コピー」のみ
- 何もない場所: メニュー自体を表示しない

---

## 3. メインコンテンツのタブ構成（解析結果エリア）

解析実行後、右ペインに結果が3タブ構成で表示される。

1. **根本原因＆要約タブ**: （エラーログ／症状説明／画像のうち2つ以上が入力された場合のみ）入力の優先順位についてのボックス → エラー概要／根本原因／対象ファイル・行番号／公式ドキュメントリンク。
2. **修正案(Diff)タブ**: Unified Diff（コードで直せる場合）またはタスクリスト（手順で解決する場合）、コピーボタン、検証チェックリスト、実ファイル適用セクション、バックアップ履歴一覧。
3. **学習メモ＆理論タブ**: 新人エンジニアが知っておくべき背景理論、再発防止のためのチェックリスト・ベストプラクティス。

いずれのタブにも、エラー種別バッジ・使用モデルバッジ・フォールバック発生時の警告バッジ・トークン使用量バッジを結果ヘッダーに表示する。

---

## 4. 今後の検討事項

- 初回起動時のオンボーディング（チュートリアル）表示の要否。
- ターミナル監視・クリップボード監視モーダルの発見しやすさ（現状はヘッダーの小さいボタンのみ）。
- Web版利用時に無効化される機能（プロジェクトフォルダ・ターミナル監視・クリップボード監視・Git連携）を、初見のユーザーがより分かりやすく認識できるようにする表示の検討。
- クリップボード監視でクリップボード上に画像（スクリーンショット）が乗ったことだけを軽量に検知し、貼り付けを促す通知の要否。
