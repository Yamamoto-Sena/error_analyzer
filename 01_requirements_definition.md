# 要件定義書：Debug Buddy（AI-Powered Debugging Assistant）

**プロジェクト名**: `Debug Buddy`
**文書バージョン**: v3.2.0
**最終更新**: 2026-09-14
**対象読者**: 新人エンジニア、メンターエンジニア、開発チーム
**目的**: 新人エンジニアがエラーログの読解・原因特定・修正・検証というトラブルシューティングの一連のプロセスを、AIの支援を受けながら安全に体験し、自立できるようになるためのデスクトップアプリケーションの要件を定義する。

> 本書は実装済みのソースコード（`src/`, `src-tauri/`）を正としてリバースドキュメント化した最新版です。旧版（v1.0.0/v2.0.0）に記載されていたPython製CLIツール（Typer/Rich/`debug-buddy`コマンド）構想は不採用となり、Tauriデスクトップアプリとして実装が進行しています。

---

## 1. プロジェクト概要

### 1.1 背景と課題
新人エンジニアが開発業務で直面する大きな障壁の一つが「エラーログの読解と原因特定」です。
- エラーログが長大・複雑で、どこが根本原因（Root Cause）か見極めにくい。
- エラーメッセージを検索しても、自身のコード文脈に合わせた解決策を見出すのに時間がかかる。
- ソースコードのバグだけでなく、**ポート重複（`EADDRINUSE`）や依存関係未解決、環境構築トラブル**などインフラ・プロセス起因のエラーで詰まりやすい。
- 生成AIに質問しても、提示された修正案を手動でコピペ・適用・動作確認するフローが煩雑で、誤って壊れたコードを反映してしまうリスクがある。
- なぜその修正が必要だったのかという「学び」が定着せず、同じエラーで再度時間を浪費してしまう。

### 1.2 目的とビジョン
本ツールは、**「ただ自動で直すツール」ではなく「デバッグの思考法を育てるアシスタント」** を目指します。
- **教育的アプローチ**: 「エラー概要」「根本原因」「学習メモ」「再発防止策」の構造化提示。
- **心理的安全性の担保**: 気負わずエラーと向き合えるよう、親しみやすいトーンのUI/UXを心がける。
- **幅広いエラー対応**: コード例外（`TypeError`等）から開発環境トラブル（ポート競合、依存関係、Docker、Git、TypeScriptコンパイルエラー等）まで網羅。
- **安全なコード適用**: 差分（Unified Diff）のプレビュー、ワンクリックコピー、オプションでの実ファイル適用と、いつでも元に戻せるロールバック機構。
- **ハイブリッドAI基盤**: Google Gemini APIによる高精度リアルタイム解析と、APIキー未設定・オフライン時でも即座に動作するローカルルールベースエンジンの併用。

---

## 2. システム構成・技術スタック

デスクトップアプリ（GUI）を主軸としたモダンなアーキテクチャを採用しています。

| レイヤー | 採用技術 | 選定理由・役割 |
| :--- | :--- | :--- |
| **デスクトップ基盤** | **Tauri v2 (Rust)** | Electronに比べメモリ消費が少なく高速起動。OSネイティブウィンドウと高いセキュリティ（ファイルI/O・OSキーチェーン・サブプロセス実行をRust側で制御）。 |
| **フロントエンドUI** | **React 19 + TypeScript** | コンポーネント指向による高い保守性、厳格な型安全性。 |
| **スタイリング** | **Tailwind CSS v4** | ライト/ダーク両対応のモダンUI。 |
| **アイコン** | **lucide-react** | 直感的で統一感のあるSVGアイコンセット。 |
| **ビルドツール** | **Vite 8** | 高速な開発サーバー（HMR）と最適化されたクライアントバンドル。 |
| **AI解析エンジン** | **Google Gemini API** | `fetch`によるREST直接呼び出し（SDK不使用）。モデルは実行時に選択・切替可能。 |
| **フォールバック解析** | **TypeScript内蔵ルールエンジン** (`src/analyzer.ts`) | APIキー未設定時やAPI通信エラー時でも、正規表現とパターンマッチにより即座に解析結果を生成。 |
| **実ファイル適用ロジック** | **Rust** (`src-tauri/src/fix_apply.rs`) | パストラバーサル対策・一意なハンク特定・バックアップ・ロールバック付きの安全な差分適用。 |
| **APIキー保存** | **OSキーチェーン**（`keyring`クレート、`src-tauri/src/secret_store.rs`） | Windows資格情報マネージャー／macOSキーチェーン／Linux Secret Service経由で平文保存を回避。 |
| **Git連携** | **`git`サブプロセス呼び出し**（`src-tauri/src/git_status.rs`） | 作業ツリーの未コミット変更を検知し警告表示。 |
| **ターミナル監視** | **Rustサブプロセス管理**（`src-tauri/src/terminal_watch.rs`） | 開発コマンドをアプリ内から実行し、出力をリアルタイム配信。 |
| **クリップボード監視** | **`tauri-plugin-clipboard-manager`のRust拡張トレイト経由**（`src-tauri/src/clipboard_watch.rs`） | OSクリップボードのテキストをバックグラウンドでポーリングし、変化をフロントエンドへ配信。他アプリ（MotionBoard・BigQuery/Google Cloud Console等）のエラーメッセージをコピーするだけで検知できる。 |
| **履歴保存** | **Local Storage** | 解析履歴・学習メモをローカルに永続化（外部送信なし）。 |
| **パッケージ管理** | **pnpm** | 高速な依存関係解決と省ディスク容量。 |
| **テスト** | **Vitest**（フロントエンド）／ **cargo test**（Rust） | ロジック層のユニットテストを主要機能ごとに整備。 |

---

## 3. ディレクトリ構成

```text
c:\develop/
├── 01_requirements_definition.md  # 要件定義書（本文書）
├── 02_introduction_spec.md        # 起動画面・UI/UX仕様書
├── README.md                      # プロジェクト概要・実行ガイド
├── package.json                   # フロントエンド依存関係定義
├── vite.config.ts                 # Vite設定（Tauri / Tailwind連携）
├── tsconfig.json                  # TypeScript設定
├── index.html                     # エントリーHTML
├── src/                           # デスクトップアプリ フロントエンド
│   ├── main.tsx                   # Reactエントリーポイント
│   ├── App.tsx                    # メインUI（入力、タブ、モーダル、トースト。約3,100行）
│   ├── App.css                    # Tailwind CSS v4 ディレクティブ・グローバルスタイル
│   ├── analyzer.ts                # ローカルルールベース解析エンジン・型定義
│   ├── gemini.ts                  # Gemini API クライアント（呼び出し・リトライ・モデル一覧取得・フォローアップ質問）
│   ├── models.ts                  # 選択可能なGeminiモデルの一覧・既定値・フォールバック順
│   ├── sanitize.ts                # Gemini送信前の機密情報マスキング
│   ├── usageTracker.ts            # 日次トークン使用量トラッキング
│   ├── historyStats.ts            # 振り返りダッシュボード向けの履歴集計ロジック
│   ├── TerminalWatchModal.tsx     # ターミナル監視モーダルUI
│   ├── ClipboardWatchModal.tsx    # クリップボード監視モーダルUI
│   ├── LogFileWatchModal.tsx      # ログファイル監視モーダルUI
│   ├── ModelDiagnosticsModal.tsx  # モデル疎通診断モーダルUI
│   ├── HistoryDashboardModal.tsx  # 振り返りダッシュボードモーダルUI
│   ├── FollowUpPanel.tsx          # 解析結果へのフォローアップ質問パネル
│   └── *.test.ts                  # Vitestユニットテスト
├── src-tauri/                     # Tauri バックエンド（Rust）
│   ├── Cargo.toml                 # Rustクレート定義
│   ├── tauri.conf.json            # ウィンドウサイズ、タイトル、アイコン等
│   └── src/
│       ├── main.rs / lib.rs       # ネイティブプロセスエントリーポイント・Tauriコマンド定義
│       ├── fix_apply.rs           # diff解析・安全なパス解決・適用ロジック（純粋関数・テスト付き）
│       ├── git_status.rs          # Git作業ツリーの汚れ判定
│       ├── terminal_watch.rs      # 開発コマンドの実行・出力ストリーミング
│       ├── clipboard_watch.rs     # OSクリップボードのテキストポーリング・変化配信
│       ├── log_file_watch.rs      # 外部プロセスが出力するログファイルへの追記監視
│       ├── encoding_util.rs       # Shift-JIS/CP932等のターミナル出力デコード共通処理
│       └── secret_store.rs        # Gemini APIキーのOSキーチェーン保存・読み込み
├── vscode-extension/               # （実験的PoC）VS Code拡張方式でのエディタ内解析の検証。
│                                    # 本体アプリとは独立した別プロジェクト（詳細は同ディレクトリのREADME.md参照）。
└── python-cli/                    # （廃止予定）旧CLI構想の名残。現行アプリの機能には含まれない。
```

---

## 4. 機能要件 (Feature Requirements)

### 4.1 ログ入力・自動検出機能
1. **テキストエリア自由入力**: エラーログ／スタックトレース欄と、任意入力の「症状説明」欄の2種類。リアルタイム文字数カウント、ワンクリック・クリア機能。両者は結合されてAIへ送信される。
2. **画像入力**: エラー画面のスクリーンショットをファイル選択、またはテキストエリアへのクリップボード貼り付け（Ctrl+V）で添付可能（最大4MB）。画像単独での解析にはGemini APIキーが必須（ローカル解析エンジンは画像非対応）。ログ/症状説明と画像を両方入力した状態でAPIキー未設定のまま解析した場合も、画像は解析対象に含まれず（テキストのみで解析）、その旨をトースト通知で明示する。
3. **入力の優先順位と、その可視化**: エラーログ／症状説明／添付画像のうち2つ以上が入力されている場合、Gemini解析はまず「これらが本当に同じ1つの問題についての情報なのか、それとも無関係な別々の話題なのか」を判定したうえで扱いを分ける。
   - **同じ問題の食い違い**（表現の違いや情報量の差）と判断した場合: 次の優先順位で統合して解析する: ①エラーログ（機械的な一次情報） ②添付画像（同じく一次情報だが解釈が必要） ③症状説明（ユーザー自身の解釈・補足）。
   - **明らかに無関係な別々の話題**（例: ログは起動時のポート競合、症状説明欄はまったく別の画面のUI不具合）と判断した場合: 優先度が最も高い情報源のみに基づいて解析し、優先度が低い方の内容はrootCause・summary・diffCode・taskSteps・learningContent等のどこにも一切反映させない（無理に1つの筋書きへ捏造しない）。
   - いずれの場合も、実際に何を優先して解析したか（矛盾や無関係と判断して除外した情報源があればそれも）を`inputPriorityNote`という専用のJSONフィールドで必ず返させる。
   - ローカル解析エンジン: まずログ欄単独で解析を試み、既知パターンに一致すればそれを採用（症状説明欄は参照しない）、一致しなかった場合のみ症状説明欄も含めて解析し直す、という決定的なルールで`inputPriorityNote`を組み立てる。画像は解析対象外のため、添付されていれば必ずその旨を注記する。
   - 2つ以上の入力があった解析結果画面では、「根本原因 & 要約」タブの先頭に専用ボックスとして`inputPriorityNote`を表示し、どの入力が実際に使われたかをユーザーが一目で分かるようにする（従来はrootCauseの文中に埋もれて分かりにくかったため独立させた）。入力が1種類のみの場合は表示しない。
4. **ワンクリック・サンプル挿入**: 初心者がすぐ挙動を理解できるよう、頻出エラーのサンプルボタンを、まだ解析結果が無い右側パネルに配置する（解析結果が出ると自動的に隠れる）。
   - `TypeError`（未定義オブジェクトへのプロパティアクセス）
   - `SyntaxError`（構文ミス）
   - `ReferenceError`（未定義変数の参照）
   - `Port競合 (1420)`（開発サーバー起動時のポート重複 `EADDRINUSE`）
   - `AttributeError (Python)`（`NoneType`属性アクセス）
5. **キーボードショートカット**: `Ctrl+Enter`（macOSは`Cmd+Enter`）で解析を実行。

### 4.2 ハイブリッド・エラー解析エンジン
1. **Gemini API解析**: APIキー設定時に優先動作。エラーの文脈を読み解き、JSON形式で構造化データ（要約・根本原因・対象ファイル・修正案・学習メモ・再発防止策等）を返却。
   - 選択モデルでの呼び出しが失敗した場合、あらかじめ定義したフォールバックモデル列へ自動的に切り替える。
   - 一時的な混雑（429/503）は同一モデルへの短時間リトライ、日次/月次のクォータ超過は即座に次モデルへ切替、404は次候補モデルへ即切替。
   - 45秒でタイムアウトし、分かりやすい日本語エラーメッセージへ変換。
   - 通信エラー発生時は自動的にローカル解析エンジンへフォールバックし、その旨をトースト通知。
2. **ローカル・フォールバック解析エンジン** (`src/analyzer.ts`): APIキー未設定時やオフライン時でも動作。以下を含む十数種類のパターンを優先順位付きで判定する。
   - ポート重複（`EADDRINUSE`）、Python `AttributeError`、モジュール未検出、`TypeError`（null/undefinedアクセス）、JSON構文エラー、`SyntaxError`、`ReferenceError`、ネットワークエラー／CORS、npm依存関係競合（`ERESOLVE`）、権限エラー（`EACCES`）、Docker関連、Git関連（未コミット変更／マージコンフリクト）、TypeScriptコンパイルエラー、JSヒープメモリ不足、Java `NullPointerException`、Rust panic（`thread '...' panicked at file:line:col:` 形式。アプリ本体(`src-tauri/`)もRust製であることを踏まえて対応）、その他汎用フォールバック。
   - スタックトレースからのファイルパス・行番号自動抽出（Pythonは末尾フレームを優先、`node_modules`等は除外）。
   - エラー種別に応じた公式ドキュメントリンクの自動提示。

### 4.3 モデル選択機能
1. Gemini APIキー設定時、利用可能なモデル一覧をAPIから動的取得し、選択ドロップダウンに反映（取得失敗時はアプリ内蔵の既定リストにフォールバック）。
2. **モデル一覧からの除外**: 本アプリの用途（テキスト解析＋画像は読み取り専用入力として1回のリクエストで構造化JSON応答を受け取る）に適さない特殊用途モデル、および実機診断で判明した「課金プランやアカウントの新旧に関係なく誰が呼んでも失敗するモデル」は一覧から自動除外する。
   - 画像/動画/音声「生成」系、TTS系、Gemini Robotics系、Live/Omni系のリアルタイム対話・ロボット制御モデル
   - Lyria系（音楽生成専用）、computer-use系（画面操作エージェント専用）、transcribe系（音声書き起こし専用）
   - deep-research系・antigravity系（`generateContent`非対応、Interactions API専用のエージェントモデル）
   - `gemini-2.5-flash`・`gemini-2.5-pro`・`gemini-2.5-flash-lite`（Google側が新規ユーザーへの提供を終了しており、一覧には出るが呼ぶと404になる）
3. 廃止済みの旧世代モデル（`gemini-1.5-*`、`gemini-2.0-flash`等）は既定候補から除外。
4. **モデル診断機能**: Gemini APIキー設定モーダル内の「登録済みモデルを診断する」から、ドロップダウンに表示されている各モデルへ実際に軽量な`generateContent`リクエストを1件ずつ送信し、「利用可」「利用不可（権限/未対応）」「利用不可（日次クォータ超過）」「判定不能（一時的な混雑）」に分類して一覧表示する（[src/gemini.ts](src/gemini.ts)の`diagnoseModels`、[src/ModelDiagnosticsModal.tsx](src/ModelDiagnosticsModal.tsx)）。一覧取得だけでは分からない「実際にそのキーで呼べるか」を可視化するための機能で、モデルの数だけAPI呼び出し（クォータ消費）が発生するため自動実行はせず、ユーザーが明示的に押した時のみ実行する（使用頻度が低いため、常設のヘッダーボタンではなくAPIキー設定の導線上に配置している）。
5. **トークン消費量の表示**: Gemini解析ごとにレスポンスの`usageMetadata`から取得したトークン数を、Google側の日次クォータ(RPD)がリセットされる太平洋時間(PT)の深夜0時を境界として集計し、モデル別にlocalStorageへ永続化する（[src/usageTracker.ts](src/usageTracker.ts)）。アプリを再起動しても本日分の集計は失われない。フォールバック時に実際に日次クォータ超過(429)を検知したモデルも同じ集計に記録し、該当モデルが1つでもあればヘッダーのバッジをアンバー表示に切り替える。ただし本機能はこのアプリ経由の送信実績の記録であり、Googleは第三者がAPIキー単位の残クォータを問い合わせられる公開APIを提供していないため、Google側の正確な残クォータそのものではない（他ツール経由の利用も含まれない）。

### 4.4 修正提案・Diff機能
1. **修正差分（Unified Diff）の生成**: コードで直せる場合はUnified Diff形式、手順（コマンド実行・再起動等）で解決すべき場合は番号付きタスクリストで提示。追加/削除行を色分け表示。
2. **コピー機能**: 「差分をコピー」／「手順をコピー」でクリップボードへ即時コピー。
3. **プレビュー適用**: 「コードに適用する（プレビュー）」ボタンで見た目上の反映イメージを表示（実ファイルは書き換えない）。
4. **実ファイルへの安全な適用（オプション機能・デスクトップ版限定）**:
   - プロジェクトフォルダを選択すると有効化。
   - 適用前に読み取り専用の事前判定（diffが実ファイルの内容と一致するか）を行い、可否と理由をUIに表示。
   - Git作業ツリーに未コミットの変更がある場合は警告を表示（ブロックはしない）。
   - ユーザーの明示的な確認モーダルを経て適用を実行。書き込み直前に必ずサーバー側（Rust）で再検証する多層防御構成。
   - 対象ファイルパスはプロジェクトルート基準に解決し、シンボリックリンク等によるルート外への逸脱を拒否。
   - 差分の一意特定に失敗する場合（一致箇所が0件または複数件）は書き込みを行わない。全ハンクが一意に特定できた場合のみ書き換える（all-or-nothing）。

### 4.5 ロールバック・バックアップ機構
1. 実ファイルへの適用前に必ずバックアップを作成してから書き換える（バックアップ失敗時は書き込み自体を行わない）。
2. ファイルごとに直近20世代までバックアップを保持し、超過分は古い順に自動削除。
3. バックアップ履歴一覧から任意の世代を選んでワンクリックでロールバック可能。ロールバックしてもバックアップ自体は削除しない（監査証跡として保持）。
4. 「バックアップをすべて削除」機能で、プロジェクトのバックアップ領域を一括整理可能（確認ダイアログあり）。

### 4.6 Git連携
1. プロジェクトフォルダ選択時、および適用/ロールバック成功直後に作業ツリーの状態（Git管理下か・未コミット変更の有無・変更ファイル数）を取得。
2. 未コミットの変更がある場合はヘッダーおよび適用確認モーダルに警告バッジを表示し、コミットや退避を促す（処理自体はブロックしない）。
3. Git未インストール環境やGit管理外フォルダではエラーとせず「判定不可」として扱うベストエフォート設計。

### 4.7 ターミナル監視機能（試験的機能・デスクトップ版限定）
1. プロジェクトフォルダ選択を前提に、任意の開発コマンド（既定: `npm run dev`）をアプリ内から実行し、標準出力/標準エラーをリアルタイム表示。
2. モーダルを閉じても監視はバックグラウンドで継続。アプリ終了時には監視プロセスを後始末する。
3. エラーシグナル（`EADDRINUSE`、Pythonトレースバック、未処理の例外拒否、`npm ERR!`/`npm error`、TypeScriptコンパイルエラー、セグメンテーションフォルト、パニック、`NullPointerException`、Gitコンフリクト等）を検知すると通知し、ワンクリックで解析欄へ取り込み可能。
4. 「自動解析」オプションを有効にすると、エラー検知時に確認なしで解析まで自動実行。
5. 日本語ロケール（Shift-JIS出力）や複数行コマンドの貼り付けにも対応できるよう、文字コードのフォールバック処理・コマンド整形処理を実装。

### 4.8 クリップボード監視機能（試験的機能・デスクトップ版限定）
1. プロジェクトフォルダの選択を必要とせず、いつでも独立してON/OFFできる（ターミナル監視とは別の監視対象・別モーダル）。
2. 開始時点のクリップボード内容を基準値として記録し、以降のテキスト変化のみを約800ms間隔でポーリング検知する（開始前からコピーされていた内容にはいきなり反応しない）。
3. 検知したテキストに対し、ターミナル監視と共有のヒューリスティック（`src/analyzer.ts`の`looksLikeErrorTextFromClipboard`）で「エラーらしさ」を判定する。ターミナル監視向けの高確度シグネチャに加え、BigQuery（Google Cloud Console）等クラウドサービスの言い回し（`denied`/`not found`/`exceeded`/`invalid`等）や日本語のエラーダイアログの文言（「エラー」「失敗」「不正」「権限がありません」等）も対象とし、20文字未満の短い文字列は誤検知防止のため除外する。ただし高確度シグネチャ（後述のWingarcエラーコード等）はこの文字数フィルタの対象外。「不正」はDr.Sum Server・MotionBoard公式マニュアル記載の実エラーメッセージを検証した結果を踏まえ、「不正な」ではなく素の形（「〜が不正です」等も拾えるように）にしている。
   - **Wingarc製品（Dr.Sum Server / MotionBoard）のエラーコード**: 「8桁の16進数・先頭が8/9/aのいずれか」（例: `80001006`）という形式を高確度シグネチャとして検知する。公式マニュアル記載の712件のエラーコード全件がこの形式に一致することを実測済みで、メッセージ本文の言い回しがバラバラでも、コードが一緒に表示・コピーされる限り検知できる。
4. 「自動解析」オプションを有効にすると、判定を通過した時点で確認なしに解析欄へセットし解析まで自動実行したうえで、モーダルも自動的に閉じる。無効時はログ欄へセットするだけに留め、ユーザーの「エラーを解析する」操作を待つ。
5. ヘッダーの「クリップボード監視」ボタン自体が、実行中は色（緑）と点滅ドットで状態を表示するため、モーダルを閉じていても現在監視中かどうかが分かる。モーダルを開き直した際は、Rust側の実際の監視状態を問い合わせて画面表示を補正する（アプリ再起動やリロードによる表示と実態のズレを防止）。
6. 対象はテキストのみ。クリップボード上の画像（スクリーンショット）の自動検知・自動添付は対象外で、従来通り解析欄への手動貼り付け（`Ctrl+V`）で対応する。
7. ターミナル監視と同時に有効化できるが、両方が同時にエラーを検知し自動解析が二重に走ることを防ぐため、既に解析処理が進行中の場合は自動解析を見送り、ログ欄へセットするだけに留める。
8. **カスタム監視ワード**: 組み込みのキーワード（英語中心）では拾えない、MotionBoard等アプリ固有の言い回しをユーザー自身が登録できる（最大20件・1件100文字まで、`localStorage`に保存）。登録した文言との一致は、20文字未満の短文除外フィルタの対象外として扱う（ユーザーが明示的に登録した文言のため誤検知リスクが低いとみなす）。全角/半角・空白の連続等の表記ゆれは`normalize("NFKC")`ベースの正規化で吸収したうえで比較する。
9. **動作テスト機能**: 実際にクリップボードへコピーしなくても、モーダル内に入力した文章がその場で検知されるか（カスタム監視ワード/組み込み判定のどちらに一致したか）を確認できる自己診断用のUI。

### 4.9 ログファイル監視機能（試験的機能・デスクトップ版限定）
1. 常駐サーバー等、このアプリから直接起動していないプロセスが出力するログファイル（例: `C:\myapp\logs\error.log`）への追記を監視する。ターミナル監視・クリップボード監視と同様、監視開始はプロジェクトフォルダ選択を前提としない。
2. 監視開始より前からファイルに書かれていた内容は対象外で、新たに追記された行のみを検知する。
3. 検知ロジックはターミナル監視と同じ（`looksLikeErrorText`）。「自動解析」オプション有効時は確認なしで解析欄へ取り込んで解析まで自動実行する。
4. モーダルを閉じても監視はバックグラウンドで継続する（クリップボード監視と同様、モーダルを開き直した際にRust側の実際の監視状態を問い合わせて画面表示を補正する）。

### 4.10 監視系メニューのUI統合
ターミナル監視・クリップボード監視・ログファイル監視の3ボタンをヘッダーに個別配置すると混雑するため、1つの「監視」ドロップダウンボタンに集約している。クリップボード監視・ログファイル監視のいずれかが実行中の場合は、集約ボタン自体に緑色＋点滅ドット表示で状態を示す。

### 4.11 修正検証機能
1. 修正案を適用後、結果に応じて次の3択から選んで報告する（以前は自動生成のチェックリスト全項目にチェックしないと「解消した」と申告できない方式だったが、確認観点が形骸化しやすかったため3択方式に変更した）。
   - **解決できた**: 提示した修正案どおりで解消した場合。即座に解消記録を残す。
   - **解決できなかった**: 「ログを再解析して検証する」機能で、修正後の再実行ログを再解析し、以下を自動判定する。
     - 同じ種類のエラーが継続している（`still-failing`）
     - 同じファイルで別種の問題が発生している（修正の副作用の可能性）
     - 別のエラーが新たに発生している
   - **違う方法で解決できた**: 提示した修正案とは別の方法で自力解決した場合。実際に行った方法を自由記述で記録してから解消記録として残す（振り返り・学習の参考情報として活用）。
2. **Gemini解析時**（「解決できなかった」選択時の再解析）: このリクエストには、直前に提示していた修正案（要約・根本原因・diffCodeまたはtaskSteps）も参考情報として含めて送信する。「今回のログはその修正を適用したうえで再実行して得られたもの」という前提をGeminiに伝えることで、既に試して効かなかった修正案を気づかず繰り返し提案してしまうことを防ぎ、「なぜ効かなかった可能性があるか」も踏まえた代替案を促す（[src/gemini.ts](src/gemini.ts)の`PreviousFixAttempt`）。
3. **ローカル解析エンジン**: 決定的なルールベースのパターンマッチであり、直前の修正案が効かなかったという文脈を考慮できない（同じエラー種別には常に同じ修正案を返す）。そのため、再検証で得られたdiffCode/taskStepsが直前のものと完全一致する場合は、「同じ内容を再提示している」旨をメッセージに明記し、手動での深掘りやGemini APIキーの設定を促す。
   - ただし、ローカル解析エンジンの汎用フォールバック（`isGenericFallbackResult`。既知パターンに一致しなかった場合の結果で、内容に関わらずerrorType/filePath/diffCodeが常に同じ固定値になる）に、前回・今回とも該当する場合は例外とする。errorTypeが偶然一致するだけで実際には無関係な別のエラーである可能性があるため、「同じエラーが継続している」と断定せず、「自動判定できなかった」旨を正直に伝える専用の判定結果を返す。
4. Gemini解析時の検証呼び出しも実際にトークンを消費するため、新規解析と同じ日次トークン集計（4.3の「トークン消費量の表示」参照）に加算する。
5. 検証結果は新たな解析結果として履歴にも記録される。

### 4.12 解析結果へのフォローアップ質問機能
1. 「修正案(Diff)」タブの検証セクションの下に、解析結果（根本原因・修正案）について自由記述で深掘り質問できるパネルを常設する（[src/FollowUpPanel.tsx](src/FollowUpPanel.tsx)）。
2. **Gemini APIキーの設定が必須**。ローカル解析エンジンは決定的なルールベースのため自由な質問には対応できず、APIキー未設定時はパネル内に案内文言のみを表示する。
3. 質問への回答は、直前に提示された根本原因・修正案（要約・diffCode/taskSteps）を文脈として生成される（[src/gemini.ts](src/gemini.ts)の`askFollowUpQuestion`）。
4. トークン消費対策として、過去の質問・回答のやり取り自体はプロンプトに含めない（毎回コンテキストとして送るのは元の解析結果のみとし、質問を重ねてもプロンプトが際限なく肥大化しないようにする）。
5. この質問でもトークンを消費し、新規解析と同じ日次トークン集計に加算される。

### 4.13 解析履歴（ナレッジ蓄積）機能
1. 解析結果をローカルストレージへ自動蓄積（保存上限100件、上限超過分は古いものから自動削除）。
2. 「エラー種別＋ファイルパス＋行番号」が一致する再発は重複排除し、発生回数を引き継いでカウント。同一エラーの再発時は警告トーストを表示。
3. ピン留め機能（ピン留め項目は保存上限の対象外）。
4. 表記ゆれ（全角/半角・大文字小文字・日英表記）を吸収した検索機能。
5. 表示モードとして「種類別ビュー」（初期状態は折りたたみ）と「時系列ビュー」を切替可能。
6. JSON形式／Markdownレポート形式でのエクスポート、および履歴の一括消去。

### 4.14 振り返りダッシュボード機能
解析履歴を集計し、ヘッダーの「振り返り」ボタンから開くダッシュボードに表示する（[src/historyStats.ts](src/historyStats.ts)、[src/HistoryDashboardModal.tsx](src/HistoryDashboardModal.tsx)）。Web版でもlocalStorage経由の履歴で利用できる。
1. **よく詰まるエラー種別**: エラー種別ごとの発生件数を多い順にバー表示。
2. **直近の学習メモ**: 直近の解析結果から学習メモを最大5件ダイジェスト表示。クリックで該当の解析結果を開ける。
3. **同じ問題の再発**: 同一箇所（エラー種別＋ファイルパス＋行番号）で繰り返し発生している問題を再発回数とともにランキング表示。
4. **解析エンジンの利用内訳**: ローカル解析エンジンとGemini解析、それぞれの利用件数・比率を表示。

### 4.15 APIキー管理・セキュリティ
1. Gemini APIキーはOSキーチェーン（Windows資格情報マネージャー／macOSキーチェーン／Linux Secret Service）に安全に保存。Web版など利用できない環境ではローカルブラウザ内にのみ保存。
2. 旧バージョンでローカルストレージに平文保存されていたキーがあれば自動的にキーチェーンへ移行。
3. APIキーはリクエストヘッダー経由で送信し、URLやログへの露出を防止。
4. 無料のGoogle AI Studioへの取得リンクをUI内に常設。
5. Gemini送信直前に、ログ内の秘密鍵・APIキー・JWT・Bearerトークン・接続文字列内パスワード・メールアドレス・パブリックIPv4/IPv6アドレス等をマスキングし、マスクした件数をUIに明示（ローカル解析エンジンでは非適用）。GitHub Personal Access Token（`ghp_`等）・Google APIキー（`AIzaSy...`）・Slackトークン（`xoxb-`等）・Stripeキー（`sk_live_`等）・接頭辞のない裸のシークレットキー（`sk-...`）など主要なクラウドサービスのトークン形式にも個別対応している。修正検証（4.11）で送信する「直前に提示した修正案」の内容（要約・根本原因・diffCode・taskSteps）も、ログ本文と同じマスキング処理を経てから送信する。

---

## 5. 非機能要件・安全設計

新人エンジニアが実務リポジトリでも安心して使用できるよう、安全性を最重視します。

1. **破壊的変更の防止**: ユーザーの明示的な確認なしにファイルを書き換えない。実ファイルへの適用は常にプレビュー・確認モーダルを経由する。
2. **多層防御**: フロントエンドでの事前チェックを信用せず、書き込み直前にRust側で必ず再検証する。
3. **原子的な書き込み**: 一時ファイルへの書き込み後にリネームすることで、書き込み途中の破損を防止する。
4. **機密情報の保護**: AIへ送信するログ内のシークレット類を送信前にマスキングする。
5. **オフライン動作の担保**: APIキー未設定・ネットワーク不通でもローカル解析エンジンで最低限の解析結果を返す。
6. **プラットフォーム互換性**: Windows・macOS・Linuxで動作すること。Web版（GitHub Pages等）ではファイルI/O・Git連携・ターミナル監視・クリップボード監視・実ファイル適用等OS依存機能を無効化し、利用不可であることをUIで明示する。
7. **UIフリーズ対策**: ネイティブダイアログの応答なしを一定時間でタイムアウトし操作可能状態へ復帰させる。
8. **日本語対応**: 全UI・メッセージを日本語化し、日本語ロケールの端末出力（Shift-JIS等）も正しく扱う。

---

## 6. UI/UX 概要

詳細は [02_introduction_spec.md](02_introduction_spec.md) を参照。

- **カラーパレット**: ライトモードは白/Slate系、ダークモードはSlate-950（背景）を基調とし、Cyan／Emerald／Indigo／Rose／Amberをステータスアクセントとして使用。
- **フィードバック**: アクションごとに画面右下にアニメーション付き**トースト通知**を表示（成功・情報・警告）。
- **レイアウト**: ヘッダー（モデル選択・APIキー設定・監視メニュー［ターミナル/クリップボード/ログファイル監視を集約したドロップダウン］・プロジェクトフォルダ・Git警告・解析履歴・振り返り・テーマ切替、をクラスタごとに区切り線で分けて配置）＋ 2ペイン構成（左: 入力／右: 解析結果を3タブで表示。まだ解析結果が無い間はサンプルボタンをここに表示）。
- **ライト/ダークモード切替**、OS設定への追従。
- **右クリックメニュー**: WebView既定のブラウザ向けメニュー（戻る/進む/検証等）は常に非表示にし、入力欄・テキスト選択上でのみ「コピー/切り取り/貼り付け」だけの自作メニューを表示する（ローカルデスクトップアプリとして自然な操作感にするための対応）。

---

## 7. 今後の拡張ロードマップ

- **完了済み**:
  - Tauri + React + Tailwind によるGUI基盤構築
  - Gemini API連携（モデル切替・フォールバック・トークン集計）＆ ローカル解析エンジン
  - 実ファイルへの安全な自動適用（Rust側での多層防御）とロールバック・バックアップ世代管理
  - Gitワーキングツリーの状態表示
  - ターミナル監視モード（試験的機能）
  - クリップボード監視モード（試験的機能。他アプリ・BigQuery等のエラーをコピーするだけで検知）
  - ログファイル監視モード（試験的機能。常駐サーバー等が出力するログファイルへの追記を検知）
  - ヘッダーの監視系3ボタンを1つのドロップダウンメニューに統合（混雑解消）
  - 右クリックメニューのローカルアプリ向け最小化（コピー/切り取り/貼り付けのみ）
  - 修正検証（再解析による解消判定、解決できた/できなかった/違う方法で解決できたの3択方式）
  - 解析結果へのフォローアップ質問機能（Gemini APIキー必須）
  - 解析履歴（重複排除・ピン留め・エクスポート・種類別ビュー）
  - 振り返りダッシュボード（エラー種別頻度・学習メモダイジェスト・再発ランキング・解析エンジン利用内訳）
  - APIキーのOSキーチェーン保存、送信前の機密情報マスキング（主要クラウドサービスのトークン形式・IPv6アドレスにも対応）
  - Rust panicのローカル解析パターン対応
- **実験的な取り組み（本体アプリとは別軌道）**:
  - `vscode-extension/`: VS Code拡張方式でのエディタ内解析のPoC（詳細は同ディレクトリのREADME.md参照）
- **今後検討**:
  - ターミナル監視のプロセスツリー完全終了への対応強化（Windows環境の既知の制約）
  - クリップボード上の画像（スクリーンショット）の自動検知・自動添付への対応
  - チーム内ナレッジ共有（解決済みエラーレポートの共有機能）
  - ローカルLLM（Ollama等）への対応検討
  - `python-cli/` として残る旧CLI構想の扱い（廃止 or 別ツールとして再整理）の決定
