# 要件定義書：エラーログ解析＆自動修正アシスタント (CLI)
# 要件定義書：エラーログ解析＆自動修正アシスタント (Debug Buddy)

**プロジェクト名**: `debug-buddy`（仮称）  
**文書バージョン**: v1.0.0  
**プロジェクト名**: `Debug Buddy`  
**文書バージョン**: v2.0.0  
**対象読者**: 新人エンジニア、メンターエンジニア、開発チーム  
**目的**: 新人エンジニアがトラブルシューティングのプロセスを体系的に学びながら、実務でのデバッグ作業を効率化できるCLIツールの要件を定義する。
**目的**: 新人エンジニアがトラブルシューティングのプロセスを体系的に学びながら、実務でのデバッグ・開発環境トラブルを自立的に解決できるアシスタントツールの要件を定義する。

---

## 1. プロジェクト概要

### 1.1 背景と課題
新人エンジニアが開発業務で直面する大きな障壁の一つが「エラーログの読解と原因特定」です。
- エラーログが長大・複雑で、どこが根本原因（Root Cause）か見極めにくい。
- エラーメッセージを検索しても、自身のコード文脈に合わせた解決策を見出すのに時間がかかる。
- 生成AIに質問しても、コード修正の適用やGit管理、動作検証のフローが属人化し、安全にコードへ反映できない。
- なぜその修正が必要だったのかという「学び」が定着せず、同じエラーで再度詰まってしまう。
- ソースコードのバグだけでなく、**ポート重複（`EADDRINUSE`）や環境構築・依存関係の未解決**などインフラ・プロセス起因のエラーで詰まりやすい。
- 生成AIに質問しても、提示された修正案を手動でコピペ・適用・動作確認するフローが煩雑。
- なぜその修正が必要だったのかという「学び」が定着せず、同じエラーで再度時間を浪費してしまう。

### 1.2 目的とビジョン
本ツールは、**「ただ自動で直すツール」ではなく「デバッグの思考法を育てるアシスタント」** を目指します。
- エラーログの構造化と根本原因のハイライト
- 発生理由・背景理論の平易な解説（教育的アプローチ）
- 安全な差分（Diff）提示と、ユーザー承認に基づくコード自動適用
- 修正後のテスト自動実行による動作担保
- 遭遇したエラーと学びのナレッジ蓄積
- **教育的アプローチ**: 「要約」「根本原因」「学習メモ」「再発防止策」の構造化提示。
- **心理的安全性の担保**: 「合言葉: エラーは成長のチャンス！」を掲げ、初心者が気負わずに使えるUI/UX。
- **幅広いエラー対応**: コード例外（`TypeError`等）から開発環境トラブル（ポート競合等）まで網羅。
- **安全なコード適用**: 差分（Unified Diff）のプレビュー、ワンクリック適用、およびいつでも元に戻せるロールバック機構。
- **ハイブリッドAI基盤**: 最新世代の **Gemini 3.8 Flash** による高精度リアルタイム解析と、オフラインでも即時動作するローカルルールの併用。

---

## 2. システム構成・技術スタック

新人エンジニアが内部実装を読み解きやすく、拡張やコントリビュートが容易なスタックを選定します。
デスクトップアプリ（GUI）を主軸とし、軽量なCLIサブシステムも備えたモダンなアーキテクチャを採用しています。

| レイヤー | 選定技術 | 選定理由 |
| レイヤー | 採用技術 | 選定理由・役割 |
| :--- | :--- | :--- |
| **開発言語** | **Python 3.11+** | 学習コストが低く、型ヒント（Type Hints）が標準化。文字列解析やLLM SDKが充実。 |
| **CLIフレームワーク** | **Typer** | 型ヒントに基づき直感的にサブコマンドや引数を定義可能。Click上位互換で初心者にも読みやすい。 |
| **ターミナルUI/装飾** | **Rich** | 構文ハイライト、Markdown描画、テーブル、差分表示（Syntax/Diff）、進捗バーを容易に実現。 |
| **データバリデーション**| **Pydantic v2** | 解析結果やLLMレスポンス（Structured Outputs）の型安全な定義とバリデーション。 |
| **LLMエンジン / SDK** | **Google GenAI SDK (Gemini)** または **OpenAI API** | 高速・安価かつ構造化出力（JSON Schema）に優れたモデルを活用。 |
| **Git操作** | **GitPython** または **subprocess (git CLI)** | 差分の取得、変更前ブランチのチェックポイント作成、安全なロールバックを実現。 |
| **設定管理** | **pydantic-settings / python-dotenv** | 環境変数（APIキー等）や設定ファイル（`config.toml` / `.env`）の安全なハンドリング。 |
| **テストフレームワーク**| **pytest**, **pytest-mock** | 高速かつ可読性の高いテスト作成・実行。CLIコマンドの統合テスト容易性。 |
| **パッケージ・環境管理**| **uv** または **Poetry** | 高速なパッケージ解決と仮想環境管理（モダンPython標準）。 |
| **デスクトップ基盤** | **Tauri v2 (Rust)** | Electronに比べメモリ消費が極めて少なく、高速起動。OSネイティブウィンドウと高いセキュリティ。 |
| **フロントエンドUI** | **React 19 + TypeScript** | コンポーネント指向による高い保守性、厳格な型安全性、モダンな状態管理。 |
| **スタイリング** | **Tailwind CSS v4** | ダークモードを基調とした洗練されたモダンUI（Glassmorphism、視認性の高い配色）。 |
| **アイコン・UI部品** | **Lucide React** | 直感的で統一感のあるSVGアイコンセット。 |
| **ビルドツール** | **Vite 8** | 超高速な開発サーバー（HMR）と最適化されたクライアントバンドル。 |
| **LLMエンジン** | **Google Gemini 3.8 Flash** | 最新世代モデル。高い論理推論力、コード理解、JSON構造化出力（Structured Outputs）の安定性。 |
| **フォールバック解析** | **TypeScript 内蔵エンジン** | APIキー未設定時やオフライン時でも、正規表現とパターンマッチにより即座に解析とアドバイスを生成。 |
| **ナレッジ保存** | **Local Storage / SQLite** | 過去のデバッグ履歴と学習メモをローカルに永続化（外部への情報漏洩防止）。 |
| **パッケージ管理** | **pnpm** | 高速な依存関係解決と省ディスク容量。 |
| **CLIサブシステム** | **Python 3.14 (Typer + Rich)**| ターミナル作業に閉じた開発者向けのパイプ連携・CLI実行環境（`python-cli/`）。 |

---

## 3. ディレクトリ構成

保守性と学習性を高めるため、責任範囲が明確なレイヤード/モジュラー構成を採用します。
保守性と責任分離を徹底したプロジェクト構成です。

```text
debug-buddy/
├── .env.example                 # 環境変数テンプレート (APIキー等)
├── pyproject.toml               # プロジェクト設定・依存関係管理
├── README.md                    # 使い方・インストールガイド
├── docs/                        # ドキュメント (設計書、チュートリアル)
│   └── 01_requirements_definition.md
├── src/
│   └── debug_buddy/
│       ├── __init__.py
│       ├── main.py              # CLIエントリーポイント (Typer appの定義)
│       ├── config.py            # 設定値・環境変数の読み込みと管理
│       ├── core/                # コアロジック (解析・修正・実行)
│       │   ├── __init__.py
│       │   ├── parser.py        # ログパーサー (スタックトレース・例外の抽出)
│       │   ├── analyzer.py      # LLM連携によるエラー原因分析・解説生成
│       │   ├── patcher.py       # 修正差分生成およびファイルへのパッチ適用
│       │   └── runner.py        # テストやコマンドの実行・検証サブプロセス
│       ├── git/                 # Git操作モジュール
│       │   ├── __init__.py
│       │   └── vcs.py           # 差分確認、自動コミット、ロールバック機能
│       ├── storage/             # 学習ログ・履歴保存モジュール
│       │   ├── __init__.py
│       │   └── history.py       # SQLiteまたはMarkdown形式でのナレッジ保存
│       └── ui/                  # ターミナル表示関連
│           ├── __init__.py
│           ├── console.py       # Richを用いた共通コンソール・スタイル定義
│           └── renderer.py      # Markdown解説、Diff、テーブルの描画
├── tests/                       # テストコード
│   ├── conftest.py              # pytest共通フィクスチャ
│   ├── fixtures/                # テスト用ダミーログ・ソースコード
│   │   ├── logs/
│   │   │   ├── python_traceback.log
│   │   │   └── npm_error.log
│   │   └── sample_project/
│   ├── test_parser.py
│   ├── test_analyzer.py
│   ├── test_patcher.py
│   └── test_cli.py
└── .gitignore
c:\develop/
├── 01_requirements_definition.md# 要件定義書（本文書）
├── 02_introduction_spec.md      # UI/UXおよび起動仕様書
├── README.md                    # プロジェクト概要・実行ガイド
├── package.json                 # フロントエンド依存関係定義
├── vite.config.ts               # Vite設定（Tauri / Tailwind連携）
├── tsconfig.json                # TypeScript設定
├── index.html                   # エントリーHTML
├── src/                         # デスクトップアプリ フロントエンド
│   ├── main.tsx                 # Reactエントリーポイント
│   ├── App.tsx                  # メインUI（入力、タブ、モーダル、トースト）
│   ├── App.css                  # Tailwind CSS v4 ディレクティブ・グローバルスタイル
│   ├── analyzer.ts              # ローカル動的解析エンジン（ポート競合・例外判定）
│   └── gemini.ts                # Google Gemini 3.8 REST API クライアント
├── src-tauri/                   # Tauri バックエンド（Rust）
│   ├── Cargo.toml               # Rustクレート定義
│   ├── tauri.conf.json          # ウィンドウサイズ、タイトル、パーミッション設定
│   └── src/
│       └── main.rs / lib.rs     # ネイティブプロセスエントリーポイント
└── python-cli/                  # 独立したCLIツール（Python版）
    ├── pyproject.toml           # Typer, Rich, Pydantic v2
    └── src/
        └── debug_buddy/
            ├── main.py          # CLIエントリーポイント
            ├── config.py        # 環境変数管理
            └── ui/banner.py     # ターミナルASCIIロゴ・バナー
```

---

## 4. 基本機能要件 (Feature Requirements)
## 4. 機能要件 (Feature Requirements)

### 4.1 ログ解析＆入力機能 (`analyze` コマンド)
1. **多様な入力方式**:
   - ログファイルを直接指定: `debug-buddy analyze app.log`
   - パイプ入力からの受け取り: `python main.py 2>&1 | debug-buddy analyze -`
   - 直前コマンドのエラー取得: `debug-buddy run "pytest"` (コマンドを内部実行して失敗時に自動解析)
2. **スタックトレース＆メタ情報の抽出**:
   - 言語・フレームワークの自動推定（Python, Node.js/TypeScript, Go, Rust等）
   - エラー種別（`TypeError`, `NullPointerException`, `SyntaxError` 等）と発生箇所のファイルパス・行番号の特定
   - 該当ソースコード周辺（前後10〜20行）の自動読み込み
### 4.1 ログ入力・自動検出機能
1. **テキストエリア自由入力**:
   - ターミナル、コンソール、ビルドログからの自由なペースト。
   - リアルタイム文字数カウント、ワンクリック・クリア機能。
2. **ワンクリック・サンプル挿入**:
   - 初心者がすぐ挙動を理解できるよう、頻出エラーのサンプルボタンを配置。
     - `TypeError`: 未定義オブジェクトへのプロパティアクセス（`reading 'map'`）
     - `SyntaxError`: 閉じ括弧やカンマの構文ミス
     - `ReferenceError`: 未定義変数の参照やimport忘れ
     - `Port競合 (1420)`: 開発サーバー起動時のポート重複（`EADDRINUSE`）

### 4.2 教育的解説機能 (Educational Explanation)
新人エンジニアの自立を促すため、LLMによる解説出力を以下の固定フォーマットで提示します。
1. **要約 (Summary)**: ひとことで言うと何が起きているか。
2. **根本原因 (Root Cause)**: なぜこの例外が発生したのか（構文、型の不一致、未定義変数、環境問題など）。
3. **学習メモ (Learning Notes)**: 
   - このエラーを防ぐためのベストプラクティスや背景理論（例: 「Pythonにおける可変デフォルト引数の罠」「非同期関数のawait忘れ」など）。
   - 公式ドキュメントへの参照リンクやキーワード。
### 4.2 ハイブリッド・エラー解析エンジン
1. **Gemini 3.8 リアルタイム解析**:
   - APIキー設定時に動作。最新モデルがエラーの文脈を読み解き、JSONフォーマットで構造化データを返却。
2. **ローカル動的フォールバックエンジン ([src/analyzer.ts](file:///c:/develop/src/analyzer.ts))**:
   - APIキー未設定時やネットワーク不通時でも自動動作。
   - **開発環境エラーの特定**: `Port ... is already in use` からポート番号を抽出し、Tauriの `ELIFECYCLE` との因果関係を解明。
   - **スタックトレース解析**: `node_modules` 内部を除外し、ユーザーが修正すべきファイルパスと行番号を優先抽出。

### 4.3 修正提案＆インタラクティブ適用機能 (`fix` コマンド)
1. **修正差分（Unified Diff）の生成**:
   - ファイルパス、変更行、追加/削除コードをRichのシンタックスハイライト付きでプレビュー。
2. **対話型プロンプト (Confirmation)**:
   - `[y] 修正を適用する`
   - `[n] スキップする`
   - `[e] 別の修正案を再生成する`
   - `[c] 詳しい理由をもっと質問する (対話モードへ)`
3. **安全な変更適用**:
   - 変更前の状態をGitのStashまたはバックアップファイル (`.bak`) として自動退避。
   - 修正適用後に差分をコミットするか、いつでもワンコマンドで差し戻し（`rollback`）可能にする。
### 4.3 教育的3ペイン結果表示
新人エンジニアの成長を促すため、解析結果を以下の3つのタブに整理して提示。
1. **根本原因 & 要約タブ**:
   - 「エラー概要」（何が起きたか）
   - 「根本原因 (Root Cause)」（なぜ起きたのかの背景）
   - 「対象ファイル・行番号」（どこを見ればよいか）
2. **修正案 (Diff) タブ**:
   - Unified Diff形式による追加（緑）/ 削除（赤）のハイライト表示。
   - 環境エラーの場合は、実行すべきターミナルコマンド（PowerShell / CMD）を明示。
   - **「差分をコピー」**: クリップボードへ瞬時にコピー。
   - **「コードに自動適用する」**: ファイルへの反映アニメーションと、いつでも元に戻せる **「ロールバック」** 機能。
3. **学習メモ & 理論タブ**:
   - 新人エンジニアが知るべき言語仕様や技術的概念（例: オプショナルチェーン `?.`、ポートとプロセスのライフサイクルなど）。
   - 再発防止のためのチェックリスト・ベストプラクティス。

### 4.4 検証＆テスト自動化機能 (`verify` コマンド)
1. **修正後テストの自動実行**:
   - パッチ適用後、プロジェクトに存在するテストコマンド（`pytest`, `npm test` 等）を自動検知または指定コマンドで実行。
2. **再帰的デバッグループ**:
   - テストが失敗した場合、新たなエラーログをもとに再度修正案を提示（最大リトライ回数を設けて無限ループ防止）。
### 4.4 ナレッジ蓄積・履歴管理 (History)
1. **自動履歴保存**:
   - 解析を実行するたびに、エラー種別、要約、対象ファイル、日時をローカルストレージに自動蓄積。
2. **履歴モーダル ([src/App.tsx](file:///c:/develop/src/App.tsx))**:
   - ヘッダーの「履歴」ボタンから過去のトラブルシュート一覧を開き、ワンクリックで前回の解説とDiffを再閲覧可能。
   - 履歴の一括消去機能。

### 4.5 学習・ナレッジ蓄積機能 (`history` / `notes` コマンド)
1. **デバッグ履歴の保存**:
   - 過去に発生したエラー、原因、適用した修正をローカル（`~/.debug-buddy/history.db` または `.debug-buddy/logs/`）に自動蓄積。
2. **チートシート・振り返り出力**:
   - `debug-buddy notes --summary`: 直近1週間で遭遇したエラー一覧と学びをMarkdown形式でエクスポートし、日報や週報、個人学習メモに活用可能。
### 4.5 APIキー管理 & セキュリティ
- 画面右上の「Gemini 3.8: APIキー設定」からいつでも入力・更新可能。
- 入力されたAPIキーは外部サーバーを介さず、ユーザーのローカルブラウザ内にのみ安全に保存。
- 無料の Google AI Studio への取得リンクをUI内に常設。

---

## 5. 非機能要件・安全設計
## 5. UI/UX 仕様

新人エンジニアが実務リポジトリでも安心して使用できるよう、安全性を最重視します。
- **カラーパレット**: Slate-950（背景）、Slate-900（カード・入力欄）、Cyan / Emerald / Rose（ステータスアクセント）。
- **フィードバック**: アクションごとに画面右下にアニメーション付き **トースト通知** を表示（成功、情報、警告）。
- **レスポンシブ**: デスクトップに最適化された2ペイン構造（左: 入力 / 右: 結果）。

1. **破壊的変更の防止**:
   - ユーザーの明示的な確認なしにファイルを書き換えない。
   - Gitワーキングツリーが汚れている（未コミットの変更がある）場合は警告を出し、バックアップ作成を促す。
2. **シークレット・機密情報の保護**:
   - ログ内に含まれるトークン、APIキー、パスワード、個人情報（メールアドレス・IPアドレス等）をLLMへ送信する前にマスキング（正規表現によるサニタイズ処理）。
3. **APIキー・コスト制御**:
   - ローカルLLM（Ollama等）への接続切り替えに対応し、完全オフライン/コストゼロでの学習利用も可能にする。
   - トークン消費量をログ・画面に表示。
4. **プラットフォーム互換性**:
   - Windows (PowerShell), macOS, Linux (bash/zsh) でパス区切りや文字コード（UTF-8）の差異なく動作すること。

---

## 6. コマンドインターフェース仕様（CLIイメージ）
## 6. 今後の拡張ロードマップ

```bash
# 1. ログファイルを解析して解説を表示
$ debug-buddy analyze error.log

# 2. ログ解析と同時に修正案を提示・適用
$ debug-buddy fix error.log --test "pytest tests/"

# 3. テストを実行し、失敗したら自動で解説と修正へ
$ debug-buddy run "pytest"

# 4. 直前の修正をロールバック
$ debug-buddy rollback

# 5. 遭遇したエラーの学習ログ一覧を確認
$ debug-buddy history list
```

---

## 7. 開発ステップ（マイルストーン）

- **Phase 1: ミニマムプロトタイプ (MVP)**
  - Typer + Rich のCLI基盤構築
  - ログファイルからの例外メッセージ抽出
  - LLM連携による原因解説とUnified Diffのターミナル出力
- **Phase 2: インタラクティブ適用と安全設計**
  - 対話形式でのファイル書き換えパッチ処理
  - バックアップ・ロールバック機構の実装
  - 機密情報のマスキング機能
- **Phase 3: 自動検証とテスト連携**
  - コマンド実行（`run`）と終了コード判定
  - 修正後のテスト自動再実行
- **Phase 4: 学習支援機能・拡張**
  - デバッグ履歴（SQLite/Markdown）管理機能
  - Ollama等ローカルLLMへの対応
  - 設定ファイル（`.debugbuddy.toml`）によるプロジェクト別カスタマイズ

- **Phase 1: GUI基本基盤＆動的解析（完了）**
  - Tauri + React + Tailwind によるUI構築
  - Gemini 3.8 連携 ＆ ローカル動的解析エンジン
  - 差分コピー、自動適用、履歴保存
- **Phase 2: 実ファイル直接書き換え＆Git連携**
  - Tauriコマンド（Rust）経由でのローカル実ファイルの安全な自動パッチ適用
  - Gitワーキングツリーの自動退避（stash/commit）とワンクリック・ロールバック
- **Phase 3: ターミナル直接監視モード**
  - 開発コマンド（`npm run dev` 等）をアプリ内から直接実行し、エラー発生時に自動キャプチャ＆解析を開始するRunner機能
- **Phase 4: チーム内ナレッジ共有**
  - 解決したエラーログと学習メモをMarkdownレポートとしてエクスポートする機能
