# 使い方ガイド（初めての方向け）

**対象読者**: 初めてDebug Buddyに触れる方

このドキュメントは、アプリを実際に触ってもらうための操作ガイドです。
細かいバージョンやブランチは気にせず、「何ができるアプリか」→「起動方法」→「画面の見方」→「主要機能ごとの使い方」の順にまとめています。

---

## 1. このアプリは何か（30秒で）

**Debug Buddy** は、新人エンジニアがエラーログを読んで原因を特定し、修正して、直ったことを確認する——という一連のデバッグ作業を、AI（Gemini）またはローカルのルールエンジンが支援するデスクトップアプリです。

- ログを貼るだけで「原因の解説」「修正diff」「学習メモ」を自動生成
- 提示されたdiffは確認の上で実ファイルに安全に適用でき、いつでもロールバック可能
- APIキーが無くても（＝お金をかけなくても）ローカルのルールベース解析で一通り動く

## 2. 起動方法

**何も入っていないパソコンから始める場合**は、まず「2.0 事前準備」でツールを揃えてください。
既にGit/Node.js/pnpm/Rustが入っている場合は「2.1 ソースコードの取得」まで読み飛ばしてOKです。

### 2.0 事前準備（初めてこのパソコンでセットアップする場合）

以下を**上から順に**インストールします（すべて無料・公式サイトから）。Windows環境を想定しています。

| # | 何を | どこから | 補足 |
|---|---|---|---|
| 1 | **Git** | https://git-scm.com/downloads | インストーラーは基本すべて「Next」でOK。GitHub上のソースコードをダウンロード(clone)するのに使う |
| 2 | **Node.js**（LTS版） | https://nodejs.org/ | JavaScript実行環境。インストーラー任せでOK |
| 3 | **pnpm** | Node.jsインストール後、ターミナル（コマンドプロンプト/PowerShell）で `corepack enable` を実行 | このプロジェクトのパッケージ管理ツール |
| 4 | **Rust** | https://www.rust-lang.org/tools/install → `rustup-init.exe` | デスクトップアプリ部分(Tauri)のビルドに必要。インストーラーは既定の「1) Proceed with installation」でOK |
| 5 | **C++ビルドツール**（Windowsのみ） | https://visualstudio.microsoft.com/visual-cpp-build-tools/ | 「Visual Studio Build Tools」をダウンロードし、インストール画面で**「C++によるデスクトップ開発」**にチェックを入れてインストール（Tauriのビルドに必要。[Tauri公式の前提条件ページ](https://tauri.app/start/prerequisites/)にも詳細あり） |

インストール後、一度PCを再起動するかターミナルを開き直してから、それぞれ正しく入ったかを確認します。

```bash
git --version
node -v
pnpm -v
rustc --version
```

すべてバージョン番号が表示されれば準備完了です（`'git' は認識されません` のようなエラーが出る場合は、そのソフトのインストールをやり直すか、PCの再起動をお試しください）。

### 2.1 ソースコードの取得（clone）

好きな場所（例: `C:\dev`）にフォルダを作り、そこにプロジェクトをダウンロードします。

```bash
cd C:\dev
git clone https://github.com/Yamamoto-Sena/error_analyzer.git
cd error_analyzer
```

> `C:\dev` フォルダがまだ無い場合は「指定されたパスが見つかりません」と表示されます。その場合は先に `mkdir C:\dev` を実行してから、もう一度上の3行を実行してください。

### 2.2 起動コマンド

`package.json` がある**プロジェクトフォルダのルート**（上でcloneしたフォルダ）で実行してください（別フォルダで実行すると後述のエラーになります）。

```bash
pnpm install          # 初回のみ。フォルダを移動/コピーした場合はそのフォルダで毎回必要
pnpm tauri dev        # デスクトップアプリとして起動（推奨）
```

- 初回起動はRustのビルドが走るため数分かかります。2回目以降は速くなります。
- ブラウザだけで軽く触りたい場合は `pnpm dev:web` も使えますが、その場合は「プロジェクトフォルダ選択」「実ファイル適用」「Git連携」「ターミナル監視」「クリップボード監視」が無効化されます（グレーアウト＋ツールチップで理由が表示されます）。**`pnpm tauri dev`（デスクトップ版）での起動をおすすめします。**

### 2.3 起動できない場合のトラブルシューティング

| 症状 | 原因 | 対処 |
|---|---|---|
| `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL` / `Command "tauri" not found` | `package.json` が無い（プロジェクトフォルダの外にいる）、または `pnpm install` を実行していない | `cd` でプロジェクトフォルダ（`package.json` がある場所）に移動し、`pnpm install` を実行してから `pnpm tauri dev` を実行する |
| `'tauri' は、内部コマンドまたは外部コマンド...` | `tauri` を直接コマンドとして実行している | `tauri` は単体のコマンドではなく `pnpm tauri dev` の形で実行する（`pnpm install` 済みのプロジェクトフォルダ内限定） |
| 別フォルダに複製/クローンして起動したい | フォルダごとに `node_modules` は独立している | 新しいフォルダで改めて `pnpm install` → `pnpm tauri dev` を実行する（`git clone` での複製で動作確認済み。特定パスへの依存はない） |

> 動作確認メモ: 本プロジェクトを `git clone` で別フォルダに複製し、そのフォルダで `pnpm install` → `pnpm tauri dev` を実行しても問題なく起動することを確認済みです。

### 2.4 Gemini APIキー（任意だが、設定した方がAI解析の使用感が分かります）
- 右上の **「🔑 Gemini AI: APIキー設定」** ボタンから設定します。
- キーは https://aistudio.google.com/app/apikey で無料取得できます（要Googleアカウント）。
- キーはOSの資格情報マネージャー（Windows）に保存され、平文ファイルには残りません。
- 未設定のままでも「ローカル解析エンジン」で一通り動作を確認できます（AIの精度自体を見たい場合はキー設定が必要）。
- **フォルダを複製/クローンした場合、APIキーはOSキーチェーン（アプリ単位ではなくOS単位）に保存されるため、元のフォルダで設定済みなら複製先でも再設定不要な場合があります。設定済みなのに「Active」表示にならない場合のみ再設定してください。**

---

## 3. 画面の見方

起動直後の画面構成（[02_introduction_spec.md](02_introduction_spec.md) より抜粋）:

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 🖥  DEBUG BUDDY                                [Gemini: gemini-3.5-flash-lite ▾] │
│    AI-Powered Debugging & Error Log Analysis Assistant 🚀                     │
│      [🔑 Gemini AI: APIキー設定] [👁 監視 ▾] [📁 プロジェクトフォルダ未選択]        │
│                    [🕘 解析履歴] [📊 振り返り] [🌙]                             │
├───────────────────────────────┬────────────────────────────────────────────┤
│  左ペイン: 入力                │  右ペイン: 解析結果                          │
│  ・エラーログ/スタックトレース  │  （結果が無い間はここにサンプルボタンを表示）│
│  ・症状の説明（任意）           │  タブ: [根本原因&要約] [修正案(Diff)] [学習メモ]│
│  ・スクリーンショット添付       │                                              │
│  ・[エラーを解析する ▶]        │                                              │
└───────────────────────────────┴────────────────────────────────────────────┘
```

| ヘッダー要素 | 説明 |
|---|---|
| モデル選択ドロップダウン | 解析に使うGeminiモデルを切替。APIキー設定時はAPIから動的取得した一覧に切り替わる |
| APIキー設定ボタン | 未設定=グレー、設定済み=エメラルドグリーン「Active」表示。設定済みの場合、モーダル内に「登録済みモデルを診断する」ボタンも表示される（→5.4） |
| 本日のトークン消費量 | Gemini APIを1回でも呼ぶと表示される。太平洋時間(PT)の深夜0時（Google側の日次クォータがリセットされる境界）を集計単位としてlocalStorageに永続化されるため、アプリを再起動しても本日分は保持される。本日中にいずれかのモデルで日次上限超過を検知していればアンバー表示になる |
| 監視ボタン（ドロップダウン） | ターミナル監視・クリップボード監視・ログファイル監視（いずれも試験的機能・デスクトップ版のみ活性）をまとめた1つのボタン。クリックで3つのモードを一覧するメニューが開く。クリップボード監視・ログファイル監視のいずれかが実行中の場合、ボタン自体が緑色＋点滅ドット表示になる |
| プロジェクトフォルダ選択 | 実ファイル適用・Git連携に必要。未選択でも解析自体は可能 |
| Git未コミットバッジ | 選択フォルダに未コミット変更があればアンバー色で件数表示 |
| 解析履歴ボタン | インディゴ色、件数バッジ付き |
| 振り返りボタン | エメラルド色。解析履歴を集計したダッシュボードを開く（→5.13） |
| ライト/ダーク切替 | 太陽/月アイコン |

---

## 4. 基本的な使い方（一番簡単な流れ）

1. 右ペイン（まだ解析結果が無い状態）のサンプルボタン（例: **TypeError**）をクリック → 左ペインにサンプルログが自動挿入される
2. 「エラーを解析する」ボタンを押す
3. 右ペインの3タブを確認する
   - **根本原因＆要約**: 何が起きたか・原因・対象ファイル/行番号・参考ドキュメントリンク
   - **修正案(Diff)**: Unified Diff形式の修正案（またはタスクリスト）＋コピー/実ファイル適用ボタン
   - **学習メモ＆理論**: なぜ起きるのか、再発防止のポイント
4. 結果ヘッダーのバッジ（エラー種別／使用モデル／フォールバック警告／トークン使用量）も確認する

自分でログを試したい場合は、左ペインのテキストエリアに任意のエラーログ・スタックトレースを直接貼り付けて「エラーを解析する」を押してください。テキストの代わりに、症状の自由記述やエラー画面のスクリーンショット（画像）を添付して解析させることも可能です（画像解析にはAPIキーが必要）。

---

## 5. 主要機能ごとの使い方

### 5.1 ログ・画像・症状説明の入力（解析の基本入力）
- 左ペインの大きなテキストエリアに、エラーログやスタックトレースをそのまま貼り付けます（`Ctrl+Enter` で解析実行も可能）。
- ログが手元に無い場合でも、その下の **「エラーの状況・症状の説明（任意）」** 欄に自由記述で状況を書けば、それだけでも解析対象になります。
- **「スクリーンショットを添付」** からエラー画面の画像ファイルを選択できます。テキストエリアに画像をそのまま貼り付け（クリップボードペースト）しても自動で添付されます（画像解析にはAPIキー設定が必要）。**APIキー未設定のままログ/症状説明と画像を両方入力して解析した場合、画像は解析対象に含まれません**（ローカル解析エンジンは画像非対応のため）。その場合は解析後のトースト通知で明示されます。
- **エラーログ／症状説明／画像のうち2つ以上を入力する場合**: まず「同じ1つの問題についての補足なのか、まったく無関係な別々の話題なのか」を判定します。同じ問題の食い違いと判断した場合は「①エラーログ ②画像 ③症状説明」の優先順位で統合して解析しますが、明らかに無関係だと判断した場合は、優先度の低い方の内容は根本原因・修正案などに一切反映されず、除外されます（適当に混ぜ合わせて1つの筋書きに仕立てることはありません）。実際に何を優先した・除外したかは、解析結果の「根本原因＆要約」タブの先頭に **「入力の優先順位について」** という専用のボックスで明示されます（入力が1種類だけの場合はこのボックス自体が表示されません）。
- 右ペイン（まだ解析結果が無い状態）の **サンプルボタン**（TypeError／SyntaxError／ReferenceError／Port競合(1420)／AttributeError(Python)）を押すと、対応するサンプルログが自動挿入されます。まず触ってみる際に便利です。

### 5.2 解析の実行と結果確認
1. 「エラーを解析する」ボタンを押す
2. 右ペインに3タブで結果が表示される
   - **根本原因＆要約**: （2つ以上の入力があった場合のみ）入力の優先順位について → エラー概要・根本原因・対象ファイル/行番号・参考ドキュメントリンク
   - **修正案(Diff)**: Unified Diff形式の修正案（コードで直せる場合）、またはタスクリスト（手順で解決する場合）。コピー用ボタン付き
   - **学習メモ＆理論**: なぜこのエラーが起きるのかという背景理論と、再発防止のポイント
3. 結果ヘッダーのバッジで、エラー種別・使用したGeminiモデル・自動フォールバック発生の有無・トークン使用量を確認できる

### 5.3 モデル選択
ヘッダーのドロップダウン（CPUアイコン）から解析に使うGeminiモデルを切替できます。
- APIキー未設定時: あらかじめ用意された固定の候補一覧が表示される
- APIキー設定時: API経由で「実際にそのキーで使えるモデル」を動的取得した一覧に切り替わる（画像/動画/音声生成系・Robotics系・Live/Omni系・Lyria系（音楽生成）・computer-use系（画面操作エージェント）・deep-research系/antigravity系（Interactions API専用）・transcribe系（音声書き起こし）、および新規ユーザーへの提供が終了した`gemini-2.5-flash`/`gemini-2.5-pro`/`gemini-2.5-flash-lite`など、このアプリの用途に合わない・誰が呼んでも失敗するモデルは自動的に一覧から除外される）
- 選択中のモデルが混雑・利用上限超過等で使えなかった場合は、自動的に別モデルへフォールバックし、その旨が結果ヘッダーに警告バッジで表示される

### 5.4 モデル診断
「一覧には出ているのに、実際に選ぶと使えないモデルがある」場合に原因を切り分けるための機能です。

1. ヘッダーの **「Gemini AI: APIキー設定」** ボタンを押してAPIキー設定モーダルを開き、**「登録済みモデルを診断する」** を押す（APIキー未設定時はこのボタン自体が表示されません）
2. モデル診断モーダルが開いたら **「診断を開始」** を押す
3. ドロップダウンに表示されている各モデルへ、実際に軽量なリクエスト（"OK"とだけ返答させる程度の最小限の内容）を1件ずつ順番に送信し、進捗（診断中... N/M）とともに以下のいずれかに分類して一覧表示する
   - **利用可**: 実際に呼び出せた
   - **利用不可（権限/未対応）**: 404/403。このAPIキーでは恒久的に呼び出せない（例: 提供終了済みの旧世代モデル、対応エンドポイントが異なるモデル）
   - **利用不可（日次クォータ超過）**: 429のうち日次/月次クォータ超過が明確なもの。プランのアップグレード等をしない限り使えない
   - **判定不能（一時的な混雑）**: 429/503を軽くリトライしても解消しなかったもの。後で再試行すれば使える可能性がある
4. 「結果をコピー」でテキストとしてまとめてコピーできる

> ⚠️ **注意**: 診断はプルダウンに表示されている**モデルの数だけ実際にAPI呼び出し（＝クォータ消費）を行います**。必要な時だけ実行してください。送信されるのは固定の短い確認用テキストのみで、入力欄のログ・画像・症状説明は送信されません。

### 5.5 修正案の実ファイル適用・バックアップ・ロールバック（デスクトップ版のみ）
1. ヘッダーの「プロジェクトフォルダ未選択」ボタンから、修正を適用したいフォルダを選択する
2. 解析結果の「修正案(Diff)」タブを開き、「実ファイルに適用する」ボタンを押す
   - diffの内容が実ファイルの中身と一致しない場合はボタンが無効化される、または適用時にエラーになります（AIの推測ミスによる誤書き込みを防ぐ安全機構）
3. 確認モーダルが開くので「バックアップを取って適用する」を選ぶ（適用前に自動でバックアップが作られる）
4. 「過去のバックアップ履歴を見る」を開くと、そのファイルの変更履歴が一覧表示される。任意の時点を選んで「元に戻す」を押すとロールバックできる（1ファイルにつき最大20世代まで保持）
5. 「バックアップを一括削除」ボタンで、選択中プロジェクトのバックアップをまとめて消すこともできる

### 5.6 Git連携（デスクトップ版のみ）
プロジェクトフォルダを選択すると、そのフォルダに未コミットの変更が無いかを自動チェックします。変更がある場合はヘッダーにアンバー色で「未コミットの変更あり（件数）」バッジが表示されます。適用処理自体はブロックされず、あくまで注意喚起です。

### 5.7 ターミナル監視（試験的機能・デスクトップ版のみ）
1. ヘッダーの「監視」ボタンからメニューを開き、「ターミナル監視」を選んでモーダルを開く
2. 監視したい開発コマンド（初期値: `npm run dev`）を入力し、開始ボタンを押す
3. コマンドの標準出力・標準エラーがモーダル内にリアルタイムで流れる
4. `EADDRINUSE` や `Traceback`、`npm ERR!` など「エラーらしい」出力を検知すると自動的にハイライトされる
5. 「自動解析」トグルをONにしておくと、エラー検知時に自動で解析欄へ取り込んで解析まで実行される。OFFの場合は手動で「解析欄へ取り込む」を選べる
6. 停止ボタンでコマンドを終了できる（Windows環境では、コマンドが起動した孫プロセスまでは終了しきれない場合がある点に注意）

### 5.8 クリップボード監視（試験的機能・デスクトップ版のみ）
MotionBoard・BigQuery（Google Cloud Console）など、Debug Buddy以外のアプリ/ブラウザで出たエラーメッセージを、貼り付け操作なしで自動検知するための機能です。

1. ヘッダーの「監視」ボタンからメニューを開き、「クリップボード監視」を選んでモーダルを開く
2. 「開始」ボタンを押す（プロジェクトフォルダの選択は不要）
3. 他のアプリ・ブラウザでエラーメッセージ等を選択して**コピー**する（`Ctrl+C`）だけでよい。このアプリへの切り替えや貼り付け操作は不要
4. コピーした内容が「エラーらしい」と判定されると（英語の`error`/`denied`/`exceeded`等や、日本語の「エラー」「失敗」「権限がありません」等を含む20文字以上のテキスト、または`80001006`のようなWingarc製品（Dr.Sum Server/MotionBoard）のエラーコード形式は文字数に関わらず）、ログ欄へ自動でセットされる
5. 「自動解析」トグルをONにしておくと、検知した時点で確認なしに解析まで自動実行され、モーダルも自動的に閉じる。OFFの場合はモーダル内のバナーから「この内容を解析欄にセットして閉じる」を選ぶ
6. **停止するには**、ヘッダーの「監視」ボタン（実行中は緑色＋点滅ドット表示）からメニューを開き、「クリップボード監視: 監視中」を選んでモーダルを開き、「停止」ボタンを押す。モーダルを閉じても監視自体はバックグラウンドで継続するため、ヘッダーボタンの色・表示で「今、監視中かどうか」を確認できる
7. 対象はテキストのみ。クリップボード上の画像（スクリーンショット）は自動検知されないため、従来通りテキストエリアへの手動貼り付け（`Ctrl+V`）で添付してください
8. ターミナル監視と同時にONにしても問題ありませんが、両方がほぼ同時にエラーを検知した場合、先に解析処理が始まっている方が優先され、後から来た方はログ欄へセットするだけに留まります（解析結果が競合して上書きされることを防ぐため）
9. **監視ワードを追加**: 組み込みの判定（英語キーワード中心）では拾えない、MotionBoard等のアプリ固有のエラー文言がある場合は、モーダル内の「監視ワードを追加」欄にその文言を登録できます（最大20件）。登録した文言を含むコピー内容は、文字数に関わらず検知されるようになります。不要になったワードは、タグ横の×ボタンで削除できます。全角/半角の数字・スペースの違いは自動で吸収されるため、コピー元の表記と多少異なっていても一致します。
10. **動作テスト**: 「監視ワードを登録したのに検知されない」場合の自己診断用に、モーダル内の「動作テスト」欄へ文章を入力すると、実際にコピーしなくてもその場で検知されるか（どの監視ワード/組み込み判定に一致したか）を確認できます。

> ⚠️ ONの間はクリップボードの内容を定期的に確認します。パスワード等の機密情報は送信前に自動マスキングされますが、意図しない内容を検知する可能性があるため、必要なときだけONにしてください。

### 5.9 ログファイル監視（試験的機能・デスクトップ版のみ）
常駐サーバー等、このアプリから直接起動していないプロセスが出力するログファイル（例: `C:\myapp\logs\error.log`）への追記を監視するための機能です。ターミナル監視・クリップボード監視と同様、モーダルを閉じても監視自体はバックグラウンドで継続します。

1. ヘッダーの「監視」ボタンからメニューを開き、「ログファイル監視」を選んでモーダルを開く
2. 「参照...」から監視したいログファイルを選択する（直接パスを入力することも可能）
3. 「開始」ボタンを押す。**監視開始より前からファイルに書かれていた内容は対象外**で、新たに追記された行のみが検知対象です
4. ファイルへの追記がモーダル内にリアルタイムで表示される
5. 追記された行が「エラーらしい」と判定されると（ターミナル監視と同じ判定ロジック）、ヘッダーボタンの下にバナーが表示される
6. 「自動解析」トグルをONにしておくと、エラー検知時に確認なしで自動的に解析欄へ取り込んで解析まで実行される。OFFの場合はバナーの「この内容を解析欄にセットして閉じる」を選ぶ
7. 停止するには、ヘッダーの「監視」ボタン（実行中は緑色＋点滅ドット表示）からメニューを開き、「ログファイル監視: 監視中」を選んでモーダルを開き、「停止」ボタンを押す

### 5.10 修正の検証
修正を適用した状態で同じ操作を再実行した後、「修正案(Diff)」タブの検証セクションで、結果に近いものを3択から選びます（以前は自動生成の確認チェックリストに全項目チェックしないと完了報告できない仕組みでしたが、確認観点が形骸化しがちだったため、結果に応じた3択方式に変更しました）。

1. **解決できた**: 押すとその場で解消記録が表示されます（追加の入力は不要）
2. **解決できなかった**: 選ぶと入力欄が開くので、再実行後に出力されたログを貼り付けて「ログを再解析して検証する」を押す。元のエラーが解消したか、あるいは別のエラーに変わっていないかを再解析して結果を表示する
   - **Gemini APIキー設定時**: この再解析には、直前に提示していた修正案（要約・根本原因・Diffまたは手順）も自動的に含めて送信されます。そのため「今回のログはその修正を試した後のもの」という前提でGeminiが判断し、既に効かなかった修正案をそのまま繰り返すのではなく、なぜ効かなかった可能性があるかを踏まえた別の案を提示しようとします。
   - **APIキー未設定時（ローカル解析エンジン）**: 決定的なルールベースのため、直前の修正案が効かなかったという文脈は考慮できません。もし再解析の結果が直前と完全に同じ修正案だった場合は、「同じ内容を再提示しています」という注記がメッセージに表示されます。その場合は手動での深掘り、またはGemini APIキーの設定をご検討ください。ただし、再実行後のログが（前回・今回とも）ローカル解析エンジンの既知パターンにまったく一致しなかった場合は、「❓ 自動判定できませんでした」という中立的なメッセージが表示されます（未知のパターン同士は内容に関わらず同じ表示になってしまうため、「同じエラーが継続している」と誤って断定しないための配慮です）
   - Gemini APIキー設定時は、この検証の再解析でもトークンを消費します。ヘッダーの「本日のトークン消費量」バッジにも、新規解析と同様に加算されます
3. **違う方法で解決できた**: 提示された修正案とは別の方法で自力解決できた場合に選びます。選ぶと入力欄が開くので、実際に行った方法を自由記述で記録してから「この内容で完了報告する」を押すと、その内容を含んだ解消記録が表示されます

### 5.11 解析結果への質問（フォローアップ）
「修正案(Diff)タブ」の検証セクションの下に、解析結果について自由記述で深掘り質問できるパネルが常設されています。**Gemini APIキーの設定が必須**です（ローカル解析エンジンは決定的なルールベースのため、自由な質問には対応できません。APIキー未設定時はパネル内に案内文言のみが表示されます）。

1. パネル下部のテキストエリアに「なぜこの修正が必要なのですか？」「他の直し方はありますか？」のように質問を入力する
2. 「質問する」ボタンを押すと、直前に提示された根本原因・修正案（要約・Diff・手順）を文脈として質問への回答が生成される
3. 過去の質問・回答はパネル内にQ&A形式で積み上がって表示される
4. トークン消費対策として、過去のやり取り自体はプロンプトに含めない（毎回コンテキストとして送るのは元の解析結果のみ）ため、質問を重ねてもプロンプトが際限なく肥大化することはない
5. この質問でもトークンを消費し、ヘッダーの「本日のトークン消費量」バッジに加算される

### 5.12 解析履歴
1. ヘッダーの「解析履歴」ボタン（件数バッジ付き）から履歴モーダルを開く
2. 直近の解析結果を最大100件までローカル保存（同一エラーは自動で重複排除され、「再発回数」としてカウントされる）
3. 表示モードを **「種類別」**（エラー種別ごとにグループ化、初期状態は折りたたみ）と **「時系列」** で切替できる
4. 検索欄でエラー種別・要約・ファイル名などから絞り込み、並び替え（新しい順/古い順）も可能
5. 各履歴項目は「ピン留め」でき、ピン留めしたものは100件上限による自動削除の対象外になる
6. 「JSONでエクスポート」「Markdownでエクスポート」から、履歴全件をファイルに書き出せる（バックアップ・チーム共有用）

### 5.13 振り返りダッシュボード
ヘッダーの「振り返り」ボタン（BarChart3アイコン、エメラルド色）から、解析履歴を集計したダッシュボードを開けます。Web版でもlocalStorage経由の履歴を使って利用できます。

1. **よく詰まるエラー種別**: エラー種別ごとの発生件数を多い順にバーで表示
2. **直近の学習メモ**: 直近の解析結果から学習メモを最大5件ダイジェスト表示。クリックすると該当の解析結果を開ける
3. **同じ問題の再発**: 同一箇所（エラー種別＋ファイルパス＋行番号）で繰り返し発生している問題を再発回数とともにランキング表示。クリックすると該当の解析結果を開ける
4. **解析エンジンの利用内訳**: ローカル解析エンジンとGemini解析、それぞれの利用件数・比率を表示

### 5.14 セキュリティ機能（送信前マスキング）
Gemini APIへログを送信する直前に、APIキー・パスワード・秘密鍵・メールアドレス・パブリックIPv4/IPv6アドレスなど機密情報らしき文字列を自動でマスキングします（プライベートIP・ループバックIPは開発情報として有用なため対象外）。GitHub Personal Access Token（`ghp_`等）・Google APIキー（`AIzaSy...`）・Slackトークン（`xoxb-`等）・Stripeキー（`sk_live_`等）・接頭辞のない裸のシークレットキー（`sk-...`）など、主要なクラウドサービスのトークン形式にも個別に対応しています。マスキングが発生した場合はトースト通知で件数が案内されます。ローカル解析エンジンのみを使う場合は外部送信自体が発生しないため、この処理は行われません。修正検証（5.9）でGeminiへ送信する「直前に提示した修正案」の内容も、ログ本文と同じマスキング処理を経てから送信されます。

### 5.15 ライト/ダークモード切替
ヘッダー右端の太陽/月アイコンで切替できます。設定はローカルに保存され、次回起動時も引き継がれます。

### 5.16 右クリックメニュー
入力欄・テキスト選択上で右クリックすると、「コピー/切り取り/貼り付け」だけの最小限のメニューが出ます（ブラウザ由来の「戻る/進む/検証」等のメニューは常に表示されません）。何もない場所を右クリックした場合はメニュー自体が出ません。

---

## 6. 既知の制限事項・注意点

- ターミナル監視は試験的機能で、Windows環境では子プロセス（例: npmが起動したnode.exe）を完全に終了しきれない場合があります（その場合は手動で `taskkill /F /IM node.exe /T` 等が必要）。
- クリップボード監視も試験的機能で、対象はテキストのみです（クリップボード上の画像は自動検知されません）。監視は一度開始するとモーダルを閉じても継続するため、停止したい場合はヘッダーの「クリップボード監視」ボタン（監視中は緑色表示）からモーダルを開き、「停止」を押してください。
- ログファイル監視も試験的機能です。監視開始より前からファイルに書かれていた内容は対象外で、新たに追記された行のみを検知します。クリップボード監視と同様、モーダルを閉じても監視は継続します。
- Web版（`pnpm dev:web`）では、実ファイル適用・Git連携・ターミナル監視・クリップボード監視・プロジェクトフォルダ選択は使えません。
- モデル診断はモデルの数だけAPI呼び出しを行うため、無料枠のクォータを消費します。頻繁に実行しすぎると、他の解析用のクォータを圧迫する可能性があります。
- 解析履歴・設定はすべてローカル保存で、他端末とは同期されません。
